package PLAYER;

import GameResource.gameConfig;

public class chestBar extends backpack implements gameConfig{

	public chestBar(player p, int y, int Imgsize_backpack, int column, int line) {
		super(p, y, Imgsize_backpack, column, line);
	}
}
